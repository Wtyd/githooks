# Configuración general de la herramienta
Options:
  smartExecution: false #Si su valor es true solo se ejecutarán las herramientas que no tengan todos los ficheros modificados excluidos de su análisis

# Herramientas que se van a ejecutar
Tools:
  - phpstan
  - check-security
  - parallel-lint
  - phpcs
  - phpmd
  - phpcpd

# Configuración de cada una de las herramientas
phpstan:
    config: './qa/phpstan-phpqa.neon'
    #memory-limit: '1G' # Examples: 1M 2000M 1G 5G
    # paths: ['./src']
    # level: 8 #level 0-8 (0 default, 8 max)

#Se ejecuta contra la raiz del proyecto
parallel-lint:
    exclude: ['vendor']

phpcs:
    paths: [bin, hooks, src, tests]
    standard: './qa/psr12-ruleset.xml' #or predefined rules: Squiz, PSR12, Generic, PEAR
    ignore: [vendor,  otrodir] # Se podría configurar en el standard directamente
    error-severity: 1
    warning-severity: 6
  
phpmd:
  rules: './qa/phpmd-src-ruleset.xml' #or predefined rules cleancode,codesize,controversial,design,naming,unusedcode
  exclude: [vendor]  # Se podría configurar en las rules directamente

phpcpd:
  exclude: ['vendor']
 